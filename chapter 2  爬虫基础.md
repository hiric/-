# HTTP
1. URI：统一资源标识符    URL：统一定位标识符
   URL是URI的子集，也就是每个URL都是URI
   
2. 超文本：即网页的源代码

3. http与https是访问资源所需的协议类型，http是用于网络传输超文本数据到本地浏览器的传输协议
   https是以安全为目标的http通道，简单讲是http的安全版，即http加入SSL层
   
4. 在浏览器中输入URL并回车，这便发起了一个GET请求，请求的参数会直接包含到URL里
   POST请求大多在提交表单时发起，其数据通常以表单的形式传输
